# Real DeepSeek Screenshots

Screenshots in this directory support the `2026-05-02` real DeepSeek evaluation.

They show:

- the final chat result with project attribution, command output, token counts and cost;
- the `tiny-ledger-lab` project lock in the project selector;
- the DeepSeek configuration screen showing that the key is configured without exposing it.

The underlying test project was disposable and lived under `/tmp`.
